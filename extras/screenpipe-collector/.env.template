# Pairing writes the real token to ~/.config/chronicle-screenpipe/config.json.
# This file documents the non-secret defaults used by the guided command.
SCREENPIPE_DIR=~/.screenpipe
SCREENPIPE_URL=http://127.0.0.1:3030
