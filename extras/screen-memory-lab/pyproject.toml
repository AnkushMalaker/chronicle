[project]
name = "screen-memory-lab"
version = "0.1.0"
description = "Research prototypes for extracting events and memories from ScreenPipe capture archives"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.0.0",
    "openai>=1.60.0",
    "pillow>=10.0.0",
    "numpy>=1.26.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
