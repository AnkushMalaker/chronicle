Entry tile on a hub/landing grid.

```jsx
<HubCard icon={<Target size={20}/>} title="Wake-Word Lab" description="Review false positives & capture wake clips." cta={<>Open lab <ArrowRight size={14}/></>} active onClick={goLab} />
```
