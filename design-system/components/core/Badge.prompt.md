Small status chip. Tone carries meaning; keep labels short.

```jsx
<Badge tone="success" icon={<ShieldCheck size={14} />}>verifier on</Badge>
<Badge tone="warning" icon={<Eye size={14} />}>collect-only on</Badge>
<Badge tone="mono">hey_hermes_f.onnx</Badge>
```
