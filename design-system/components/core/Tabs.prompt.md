Two flavours. Pills for in-panel buckets; underline for the primary view toggle.

```jsx
<Tabs variant="pill" value={b} onChange={setB} tabs={[{value:'pending',label:'Pending review'},{value:'pos',label:'Positives (wake)'}]} />
```
