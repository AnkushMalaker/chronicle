Confirm dialog.

```jsx
<Modal open={o} onClose={close} title="Permanently delete audio?" icon={<AlertTriangle/>} footer={<><Button variant="ghost">Cancel</Button><Button variant="danger">Delete audio</Button></>}>The audio bytes will be deleted…</Modal>
```
